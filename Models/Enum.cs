namespace Calculator.Models;

public enum Operator
{
    Add = 0,
    Minus = 1,
    Multiply = 2,
    Divide = 3,
}

public enum ThemeMode
{
    Light,
    Dark,
    SyncWithSystem
}

public enum SupportedLanguage
{
    English,
    Chinese,
    Japanese
}